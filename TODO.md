# 待办事项

## Bug修复

### 生产线名称显示为undefined

**问题描述**：
在设备审批弹窗中，生产线下拉选项显示为 `undefined (设备ID)`，而不是生产线名称。

**问题位置**：
- 页面：设备审批页面
- 组件：生产线选择下拉框

**问题原因**：
前端在获取生产线列表时，使用了错误的字段名。后端返回的字段是 `line_name`，但前端可能期望的是其他字段名。

**修复方案**：
1. 检查设备审批页面中获取生产线数据的代码
2. 确保正确映射 `line_name` 字段到下拉选项的 label

**截图参考**：
```
设备审批弹窗中生产线下拉选项显示：
- undefined (cfd4c5ba-97b8-4a62-a370-f0d8153e041e)
- undefined (8640ed2d-3e90-4b0a-8397-dbc15b8daafe)
- ...
```

---

## 已完成

1. ✅ 设备历史状态查询修复 - 修复API路径和字段映射
2. ✅ 卡片视图指标显示修复 - 替换磁盘使用率为网络延迟
3. ✅ 布局适配修复 - 调整网格列数
4. ✅ 生产线管理编辑修复 - 添加selectedLine赋值
5. ✅ 生产线列表字段映射修复 - 适配后端返回的line_name/line_code字段
6. ✅ 设备历史状态图表生产线名称修复 - 使用line_name字段
7. ✅ 生产线详情管理员字段修复 - 从详情接口获取line_manager并映射用户名
